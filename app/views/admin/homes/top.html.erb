  <div class ="container">
   <div class ="row" >


    <h1>注文履歴一覧</h1>
    <table class = "table" >
     <div class "col">
      <thead>
       <tr>
        <th>購入日時</th>
        <th>購入者</th>
        <th>注文個数</th>
        <th>注文ステータス</th>
       </tr>
      </thead>
      <tbody>
       <% @order.each do |order| %>
       <tr>
        <td><%= order.name %></td>
        <td><%= order.created_at %></td>
        <td><%= order.order_details.amount %></td>
        <td><%= order.status %></td>
       </tr>
       <% end %>

      </tbody>
     </div>
    </table>
   </div>
  </div>
